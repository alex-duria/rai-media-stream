# ===========================================
# Project Memory Agent - Environment Config
# ===========================================

# ----- Required API Keys -----

OPENAI_API_KEY=sk-your-openai-api-key
RECALL_API_KEY=your-recall-api-key

# ----- Recall.ai Configuration -----

# Region where your Recall.ai account is hosted
RECALL_REGION=us-west-2

# ----- Public URLs -----
#
# For LOCAL DEVELOPMENT with ngrok:
#   1. Run: ngrok http 8000
#   2. Copy the https URL (e.g., https://abc123.ngrok.io)
#   3. Set BOTH CLIENT_URL and SERVER_URL to the same ngrok URL
#
# For PRODUCTION:
#   Set both to your deployed domain (e.g., https://your-app.example.com)
#
# Why same URL? The FastAPI server serves both:
#   - The client (output media page) at /
#   - The WebSocket endpoint at /ws/{project_id}

CLIENT_URL=http://localhost:8000
SERVER_URL=http://localhost:8000

# ----- Data Storage -----

# Directory for vector cache and action items
# In production, mount a persistent volume here
DATA_DIR=data

# ----- RAG Settings (optional) -----

# Minimum similarity score for search results (0.0 - 1.0)
# RAG_SIMILARITY_THRESHOLD=0.65

# Maximum number of results to return per query
# RAG_TOP_K=5

# Size of transcript chunks in characters
# CHUNK_SIZE=500

# Overlap between chunks for context continuity
# CHUNK_OVERLAP=50
